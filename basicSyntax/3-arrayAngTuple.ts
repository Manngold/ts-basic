{
  // Array
  // 배열을 나타내는 방식에는 type[]와 Array<type> 방식이 존재한다.

  const strArr: string[] = ['hi', 'hello'];
  const numArr: Array<number> = [1, 2, 3];

  //Tuple
  //배열의 요소들에게 여러 타입을 부여

  const tuple: [number, string] = [1, 'hi'];
}
